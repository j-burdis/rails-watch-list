<div class='banner'>
	<div id='emoji'>
	<%= "🎥" %>
	</div>
	<div>
		<%= link_to "My lists", root_path %>
		<%= link_to "New list", new_list_path %>
	</div>
</div>

<div class="card-category">
  Never lose track of your watch list 🎬
</div>

<div class='container'>
	<h4> My Lists </h4>

	<div class='row content'>
		<div class='col-9'>
			<div class="cards">
				<% @lists.each do |list| %>
					<div class="card-category" style="background-image: url(<%= list.photo.attached? && list.photo.key.present? ? cl_image_path(list.photo.key) : asset_path('default_image.png') %>)">
						<%= link_to list.name, list_path(list) %>
					</div>
				<% end %>
			</div>
		</div>

		<div class="col-3 btn-container">
      <%= link_to "Create a movie list", new_list_path, class: 'btn btn-success' %>
    </div>
	</div>
</div>