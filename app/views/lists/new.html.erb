<h4> New List </h4>

<div>
<%= simple_form_for [@list] do |f| %>
  <%= f.input :name %>
  <%= f.submit "Create list", class: "btn btn-primary" %>
<% end %>
</div>

<%= link_to "Back to lists", root_path %>